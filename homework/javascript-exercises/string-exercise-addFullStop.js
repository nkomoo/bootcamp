// 1. Create a function called addFullStop
// 2. It should take in a string and the output should always finish with a full stop. Some of the input strings will already have a full stop and others will not.

function addFullStop(string){
return string;
}

let string = "Hello";
console.log(string + ".");
