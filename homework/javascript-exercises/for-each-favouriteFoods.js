// 1. Create an array of your favourite foods
// 2. Use forEach to iterate over the array console logging each item on the list.

const favouriteFoods = ["Lasagna", "Sushi", "Prawn Katsu"];

function faveFoodsArr(arr) {
arr.forEach((lists) =>{
console.log(lists);
})
}
console.log(faveFoodsArr(favouriteFoods));
