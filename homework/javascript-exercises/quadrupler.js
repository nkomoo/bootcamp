// 1. Create a function called “quadrupler”
// 2. The function should take in one parameter
// 3. The function should return 4 times the parameter
// 4. Try console logging the function with different numbers as parameters

function quadrupler(fourTimes) {
return fourTimes * 4;
}
console.log(quadrupler(3));

// returns 3 * 4 = 12
